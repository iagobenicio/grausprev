class Weatherobjects {
  int temp;
  int humidity;
  String city, description, windSpeed, tempMax, tempMin;
  List<dynamic> nextdays;
  Weatherobjects(this.temp, this.city, this.description, this.humidity,
      this.windSpeed, this.nextdays);
}
